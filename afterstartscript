echo "You may need to enter your user's password multiple times now, press ENTER to continue"
read CONFIRM
git clone https://aur.archlinux.org/yay.git
cd yay && extra-x86_64-build && makepkg -i && cd .. && rm -r yay

git clone https://aur.archlinux.org/auracle-git.git
cd auracle-git && extra-x86_64-build && makepkg -i && cd .. && rm -r auracle-git

git clone https://aur.archlinux.org/pacaur.git
cd pacaur-git && extra-x86_64-build && makepkg -i && cd .. && rm -r pacaur-git

